from setuptools import setup, find_packages

setup(
    name='sandboxkit',
    version='0.1.0',
    description='A reusable utility toolbox for use in isolated Python playgrounds.',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
